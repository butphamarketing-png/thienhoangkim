/** Kích thước chuẩn slideshow.1 — mọi slide hiển thị trong cùng khung này (object-cover). */
export const HERO_SLIDE_WIDTH = 1942;
export const HERO_SLIDE_HEIGHT = 809;
export const HERO_SLIDE_ASPECT_RATIO = HERO_SLIDE_WIDTH / HERO_SLIDE_HEIGHT;
export const HERO_SLIDE_SIZE_LABEL = `${HERO_SLIDE_WIDTH}×${HERO_SLIDE_HEIGHT}px`;
